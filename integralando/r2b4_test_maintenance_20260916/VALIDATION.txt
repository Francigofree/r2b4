Validation performed while building this package:
- Source-first inspection of current GitHub main.
- Baseline Git blob SHAs embedded for all seven modified test files.
- Installer performs exact-anchor and blob-SHA safety checks before any write.
- apply.py syntax compiled successfully in the build environment.

Not performed here:
- Actual Raspberry Pi repository pytest execution (the live repo is not mounted in this environment).
- Physical robot movement.

After installation, run the targeted pytest suite and then the complete suite on the Raspberry Pi.
