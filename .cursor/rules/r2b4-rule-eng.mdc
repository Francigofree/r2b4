---
alwaysApply: true
---
R2B4 session start:
1. Run `bash scripts/bootstrap_guard.sh --print`; stop on failure.
2. Follow root `AGENTS.md`.
3. Re-read `project_rules/current_state.md` and `project_rules/current_change.json` after session or context changes.
